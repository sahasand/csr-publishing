# SQLite database file path (Railway volume mount: /data/csr.db)
DATABASE_URL="file:./dev.db"

# Directory for uploaded files (Railway volume mount: /data/uploads)
UPLOAD_DIR="./uploads"

# Directory for exported eCTD packages (Railway volume mount: /data/exports)
EXPORTS_DIR="./exports"

# Node environment (optional - defaults to development)
# NODE_ENV="production"
